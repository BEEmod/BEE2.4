async_generator>=1.10; python_version < '3.10'  # aclosing backport.
attrs >= 22.1.0
mistletoe >= 1.0.1
Pillow == 9.5.0  # 10.0 removes 32-bit wheels.
pyglet == 1.5.18  # Later versions have some issues on 3.8
PyInstaller >= 6.1.0
pygtrie >= 2.4.0
babel >= 2.12.1
trio >= 0.25.0
outcome >= 1.3.0  # Trio dependency
exceptiongroup >= 1.1.1
versioningit >= 2.1.0
srctools >= 2.3.16
rtree >= 0.9.7
typing_extensions >= 4.9.0
hypercorn>=0.14.3
quart>=0.19.4
quart-trio>=0.11.1
